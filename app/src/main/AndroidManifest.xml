<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adminmanagermvvm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feature.login.ui.UserLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.addUserInfo.readEmployeeInfo.ui.ReadEmployeeList" />
        <activity android:name=".feature.addUserInfo.readEmployeeInfo.EmployeeList" />
        <activity android:name=".feature.addUserInfo.ui.AddEmployeeInfo">

        </activity>
    </application>

</manifest>